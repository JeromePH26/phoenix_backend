name: phoenix_backend
description: PHOENIX backend for football and tennis on Railway.
version: 0.1.0
publish_to: none
environment:
  sdk: '>=3.3.0 <4.0.0'
dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  http: ^1.2.2
  postgres: ^3.4.5
  crypto: ^3.0.6
  logging: ^1.3.0
dev_dependencies:
  lints: ^5.1.1
